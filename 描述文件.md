# 一、整体架构设计（分层架构）
采用三层架构

+-----------------------+
|   CLI 用户交互层       | ← 命令行界面（纯 Python input/print）
+-----------------------+
|   业务逻辑层 (Service) | ← 核心业务：增删改查、库存扣减、销售统计等
+-----------------------+
|   数据访问层 (DAO/Repository) | ← 与 MySQL 交互，封装 SQL 操作
+-----------------------+
|   数据库 (MySQL)        |
+-----------------------+
✅ 未来扩展（加 Web API 只需替换 CLI 层）

# 二、核心功能模块划分
分为以下 4 个核心模块：
## 1. 书籍信息管理（Book Management）
添加/删除/修改/查询书籍
字段建议：
id (主键)
isbn (唯一)
title, author, publisher
price (售价), cost_price (进价，可选)
category, publish_date
description (可选)
## 2. 库存管理（Inventory Management）
记录每本书的当前库存数量
支持入库（进货）、出库（销售或损耗）
字段建议（可单独建表）：
book_id (外键)
stock_quantity
last_updated
## 3. 销售管理（Sales Management）
记录每一笔销售订单
支持销售时自动扣减库存
字段建议（sales 表）：
id
book_id
quantity
total_price
sale_time
customer_name (可选)
## 4. 统计报表（Reporting）
日/月/年销售额
热销书籍 Top N
库存预警（低于阈值提醒）
利润估算（如果记录了进价）
# 三、代码结构（项目目录）

bookstore/
├── main.py                 # 程序入口，CLI 主循环
├── config.py               # 数据库配置（host, user, password, db_name）
├── models/                 # 数据模型（可选，用字典或 dataclass）
│   └── __init__.py
├── dao/                    # 数据访问对象（DAO）
│   ├── __init__.py
│   ├── book_dao.py
│   ├── inventory_dao.py
│   └── sales_dao.py
├── service/                # 业务逻辑层
│   ├── __init__.py
│   ├── book_service.py
│   ├── inventory_service.py
│   └── sales_service.py
└── utils/                  # 工具类（如输入验证、日期格式化）
    └── validators.py


# 四、关键设计细节
1. 数据库连接管理
使用 mysql-connector-python 或 PyMySQL
待定：封装一个 DatabaseConnection 单例或上下文管理器，避免频繁开关连接

```python

# 示例：context manager
from contextlib import contextmanager
import mysql.connector

@contextmanager
def get_db_connection():
    conn = mysql.connector.connect(...)
    try:
        yield conn
    finally:
        conn.close()

```

2. 事务处理（重要！）
销售操作必须 原子性：先检查库存 → 扣库存 → 插入销售记录
使用数据库事务（conn.start_transaction() / commit() / rollback()）

3. 输入验证
CLI 层对用户输入做基本校验（如数量 > 0，价格为数字等）
防止 SQL 注入：永远使用参数化查询

```python
cursor.execute("SELECT * FROM books WHERE id = %s", (book_id,))
```

4. 异常处理
自定义异常（如 InsufficientStockError, BookNotFoundError）
在 Service 层抛出，在 CLI 层捕获并友好提示
# 五、CLI 交互设计建议
使用 菜单驱动 方式：

=== 书店管理系统 ===
1. 书籍管理
   1.1 添加书籍
   1.2 查询书籍
   1.3 修改书籍
   1.4 删除书籍
2. 库存管理
   2.1 查看库存
   2.2 入库
3. 销售管理
   3.1 新建销售
   3.2 查看销售记录
4. 统计报表
   4.1 日销售额
   4.2 热销榜
0. 退出
请选择操作：
可用递归函数或状态机实现多级菜单
# 六、后续可扩展方向（预留）
加日志（logging 模块）
支持 CSV 导入/导出
多用户权限（管理员/店员）
定时备份数据库
# 七、开发顺序建议（MVP 思路）
先建数据库表（books, inventory, sales）
实现 DAO 层基础 CRUD
实现 BookService + CLI 菜单（先跑通书籍管理）
加库存和销售，重点处理事务
最后做统计功能

